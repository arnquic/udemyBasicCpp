#ifndef UTILS_H_
#define UTILS_H_

// Prototype: a function declaration before it's definition.
// Tells the compiler that the function will be defined below and may be used before it is defined.
void doSomething();

#endif